{
  "name": "usage-monitor",
  "productName": "Usage Monitor",
  "version": "0.3.4",
  "license": "MIT",
  "author": "Errr0rr404",
  "repository": {
    "type": "git",
    "url": "https://github.com/Errr0rr404/usage-monitor.git"
  },
  "description": "Floating desktop meter for Grok, MiniMax, Codex, Claude, Cursor, Copilot, Gemini, and Muse usage.",
  "main": "electron/main.cjs",
  "scripts": {
    "start": "electron .",
    "test": "node --test lib/parse.test.cjs lib/login-capture.test.cjs lib/oauth.test.cjs lib/tray.test.cjs lib/tray-icon.test.cjs",
    "dist": "electron-builder --publish never",
    "dist:mac": "electron-builder --mac dmg zip --arm64 --x64 --publish never && node scripts/notarize-mac-disk-images.cjs",
    "dist:win": "electron-builder --win nsis --x64 --ia32 --publish never"
  },
  "build": {
    "appId": "app.usage.monitor",
    "productName": "Usage Monitor",
    "directories": {
      "output": "dist"
    },
    "artifactName": "Usage-Monitor-${version}-${os}-${arch}.${ext}",
    "files": [
      "electron/**/*",
      "lib/**/*",
      "renderer/**/*",
      "build/icon.png",
      "build/icon.ico",
      "package.json",
      "!lib/**/*.test.cjs"
    ],
    "mac": {
      "category": "public.app-category.utilities",
      "icon": "build/icon.png",
      "hardenedRuntime": true,
      "notarize": true,
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.inherit.plist",
      "extendInfo": {
        "LSUIElement": true
      },
      "target": [
        {
          "target": "dmg",
          "arch": [
            "x64",
            "arm64"
          ]
        },
        {
          "target": "zip",
          "arch": [
            "x64",
            "arm64"
          ]
        }
      ]
    },
    "win": {
      "icon": "build/icon.ico",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64",
            "ia32"
          ]
        }
      ]
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false,
      "buildUniversalInstaller": false
    }
  },
  "devDependencies": {
    "electron": "^35.7.5",
    "electron-builder": "^26.15.3"
  },
  "allowScripts": {
    "electron@35.7.5": true,
    "electron-winstaller@5.4.0": true
  }
}
